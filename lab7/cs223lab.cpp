#include <iostream>
#include <string>
#include "cs223lab.h"

// global variable for turning on/off verbose output
bool verboseout = false;

// prints the given string plus endl
void print(std::string str) {
    std::cout << str << std::endl;
}
